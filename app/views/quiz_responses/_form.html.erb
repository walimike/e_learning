<%= form_with(model: quiz_response) do |form| %>
  <% if quiz_response.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quiz_response.errors.count, "error") %> prohibited this quiz_response from being saved:</h2>

      <ul>
        <% quiz_response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quiz_id, style: "display: block" %>
    <%= form.text_field :quiz_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :selected_option_id, style: "display: block" %>
    <%= form.text_field :selected_option_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
